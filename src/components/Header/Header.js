import React from "react";
import "./Header.css";

const Header = () => (
    <div className="row mx-3 header">
        <div className="col-sm-12">
            Click on an image to earn points - but don't click on any more than once!
        </div>
    </div>
);

export default Header;